# Session: 2026-05-04

**Date**: Monday 2026-05-04 18:00–23:30 (CEST)

## Summary
Migrated the Vaultmark backend from Python/FastAPI to Next.js Route Handlers (TypeScript), eliminating the need for a separate backend service. The entire app now deploys as a single unit on Vercel (free) or Docker. Also reorganized the S3 vault structure and prepared for Vercel deployment.

## Key Decisions
- Abandoned Fargate+RDS deployment (stack was ROLLBACK_FAILED, too complex for personal project)
- Chose Vercel + S3 as deployment target instead of Fargate/ECS
- Migrated all 4 API endpoints to Next.js Route Handlers (no Python needed)
- S3 vault restructured: `wiki/` renamed to `vaultmark/` (the vault IS the wiki, no redundant folder)
- Auth: will use middleware with shared secret (free) instead of Vercel Pro password protection ($20/mo)
- `raw/` stays visible in Unlisted folder (not hidden)
- Created dedicated IAM user `vaultmark-vercel` with read-only S3 + Bedrock invoke permissions

## Outcomes
- Created `web/lib/s3.ts`, `vault-tree.ts`, `search.ts` (ports of Python modules)
- Created 4 Route Handlers: `/api/vaults`, `/api/vaults/[id]/tree`, `/api/docs/[...id]`, `/api/search`
- Updated `web/lib/api.ts` to use relative `/api/` paths
- Fixed `page.tsx` to call vault-tree directly (server-side fetch with relative URL doesn't work)
- Code review: fixed regex injection in s3.ts, race condition in search.ts, added concurrency control, env validation, region default consistency
- Updated `index.md` in S3: `wiki/` → `vaultmark/`, added `articles/`
- Created IAM user `vaultmark-vercel` (key: `AKIA4P24OBDDSDPSGHVT`)
- Added metadata, favicon.svg, logo.svg, og.svg for social sharing
- Updated README, docker-compose, .env.example for single-app architecture
- Build passes, all endpoints tested against real S3 vault

## Open Items
- [ ] Deploy to Vercel (set env vars in dashboard, connect repo)
- [ ] Add auth middleware (option 2: shared secret cookie)
- [ ] OG image may need PNG version (some platforms don't render SVG)
- [ ] Delete stuck CloudFormation stack in eu-central-1 (disable RDS deletion protection first)
- [ ] Delete unused ACM cert in us-east-1
- [ ] Consider converting og.svg to dynamic OG image route for better platform support
- [ ] `api/` directory still in repo as archived reference — decide if to remove or keep
