# Session — 2026-05-03 (continued)

**Date:** 2026-05-03 ~17:12–21:01 CEST

## Summary

Completed Phase 2 (Real Read Path) in full — all 6 tasks implemented, two code review passes done with all issues fixed, and the demo is working end-to-end. The portal now reads real Markdown from S3, renders it via remark, and searches it via in-memory fuzzy search (no Postgres required for demo).

## Key Decisions

- GET /docs/{id} returns raw Markdown; rendering happens in Next.js RSC via remark + rehype-sanitize
- SanitizedHtml branded type enforces at compile time that dangerouslySetInnerHTML only receives sanitized output
- LiveDoc discriminated union (kind: live) added to Doc type — replaces all `as never` casts
- In-memory fuzzy search (rapidfuzz) built from index.md on startup — Postgres not required for demo
- Search router tries Postgres FTS first, falls back to fuzzy search automatically
- boto3 client cached at module level; S3 calls wrapped in asyncio.to_thread
- asyncio.gather + Semaphore(10) for concurrent S3 fetches in indexer
- Shared compute_checksum utility in app/utils.py used by both docs.py and indexer.py
- DB engine + session factory cached at module level (not per-request)

## Outcomes

**API (api/)**
- app/routers/docs.py — GET /docs/{id}, async, frontmatter parsing, checksum
- app/routers/search.py — Postgres FTS with rapidfuzz fallback
- app/routers/vaults.py — GET /vaults, GET /vaults/{id}/tree (from Task 1)
- app/db/session.py — cached async engine + session factory
- app/db/models.py — SQLAlchemy Document model
- app/db/migrations/ — Alembic with GIN FTS index
- app/indexer.py — concurrent S3 fetch + Postgres upsert
- app/mem_search.py — in-memory fuzzy search from index.md
- app/utils.py — shared compute_checksum
- app/main.py — lifespan startup: DB indexer + mem_search.build_index
- api/Makefile — dev/test/lint/typecheck targets
- api/Dockerfile — uv, runs migrations then fastapi

**Frontend (web/)**
- lib/api.ts — typed fetch client (getTree, getDoc, search)
- lib/markdown.ts — remark + rehype-sanitize pipeline returning SanitizedHtml
- lib/markdown-preview.tsx — old renderer kept for editor live preview
- lib/mock/data.ts — LiveDoc + SanitizedHtml types added
- app/page.tsx — RSC fetches tree server-side
- components/app-shell.tsx — fetches real docs on click, LiveDoc state
- components/sidebar.tsx — renders real vault tree from API
- components/doc-reader.tsx — isLiveDoc guard, renders _html safely
- components/search-palette.tsx — API-backed search with 150ms debounce
- components/toc.tsx — updated to accept Doc (not just AuthoredDoc)
- web/Dockerfile — Next.js standalone output
- web/next.config.ts — output: standalone

**Infra / CI**
- infra/docker-compose.yml — Postgres 17 + api + web with health checks
- infra/.env.example
- .github/workflows/ci.yml — two jobs: web and api

**S3**
- Bucket vaultmark created (us-east-1, public access blocked)
- 15 real project docs uploaded to project-vaultmark/ prefix
- index.md nav file uploaded

**ROADMAP.md** — Phase 2 marked ✓

**Verification:** ruff clean, pyright 0 errors, 9/9 API tests, web typecheck clean

## Open Items

- Phase 3: Ingest Pipeline (MVP 1) — revive legacy/wiki.py as containerized worker
- Phase 4: Personal Wiki CRUD (MVP 2)
- Phase 5: Ask-wiki agent (Bedrock Nova 2 Lite)
- Postgres search only works when docker compose db is running; fuzzy search covers demo
- Editor and Chat remain mock-backed until Phase 4/5
