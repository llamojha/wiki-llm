# Session: 2026-05-04

**Date**: Sunday 2026-05-03 21:00 – Monday 2026-05-04 09:05 (CEST)

## Summary
Discussed Phase 3 (Ingest Pipeline) of Vaultmark and its role in the roadmap. Populated the S3 vault (`s3://vaultmark/project-vaultmark/`) with markdown docs from personal projects and published articles. Then designed and began deploying the production infrastructure on AWS using CDK.

## Key Decisions
- S3 layout: `raw/projects/<project>/` for personal project docs, `articles/` for published articles (not processed by ingest)
- Existing `wiki/` files in S3 not yet moved to `raw/` — deferred
- Deployment: Fargate + ALB + Cognito (auth at ALB listener level) on `eu-central-1`
- ACM cert for `vaultmark.amllamojha.com` in `eu-central-1` (CNAME validated via Vercel)
- `infra-cdk/` is gitignored — deployment config kept out of repo for open source
- IAM: `llamojha-local-cli` user given `AdministratorAccess` (personal dev account)
- Fargate services can be stopped/started via `infra-cdk/scripts/vaultmark.sh` to save cost (~$8-9/mo when running a few hours/day)

## Outcomes
- Uploaded ~170 `.md` files from 62 personal projects to `raw/projects/`
- Uploaded 8 article files (4 articles × en/es) to `articles/`
- Updated `README.md` with current status, stack table, S3 layout, deployment section
- Created `infra-cdk/` CDK TypeScript project (gitignored) with:
  - `lib/infra-cdk-stack.ts` — VPC, RDS t3.micro, ECS Fargate (web + api), ALB, Cognito, ECR repos, IAM roles (S3 + Bedrock + Secrets Manager)
  - `bin/infra-cdk.ts` — entry point with cert ARN and eu-central-1 region
  - `scripts/vaultmark.sh` — start/stop Fargate services + optional RDS
  - `scripts/push.sh` — build and push Docker images to ECR
- CDK bootstrapped in `eu-central-1`
- Stack deployment in progress (`CREATE_IN_PROGRESS`) — RDS still creating

## Open Items
- [ ] Wait for stack to complete (`CREATE_COMPLETE`) — check CloudFormation console
- [ ] Build and push Docker images: `./scripts/push.sh all` (images currently placeholder)
- [ ] Update `scripts/vaultmark.sh` with real cluster/service names from CDK outputs
- [ ] Add CNAME in Vercel: `vaultmark.amllamojha.com` → ALB DNS (from CDK output `AlbDns`)
- [ ] Create Cognito user for yourself: `aws cognito-idp admin-create-user --user-pool-id <id> --username your@email.com`
- [ ] Move existing `wiki/` files in S3 to `raw/` (deferred from this session)
- [ ] Delete unused `us-east-1` ACM cert (`arn:aws:acm:us-east-1:858650446023:certificate/882239fb-7ef3-4496-862a-7825bdb5f129`)
