# Session — 2026-05-15 23:10

## Summary

Fixed multiple UI and API issues in the Vaultmark portal: sidebar icon layout, 400 errors from title-cased space names, upload path selection, streaming reindex with progress, and introduced a vault structure schema (`structure.json`) to prevent reindex from destroying the user's tree layout.

## Key Decisions

- Sidebar upload/create icons wrapped in flex container to match portal wireframe (gap: 2px)
- Spaces passed as lowercase S3 keys (from folder IDs) instead of display names — fixed 400s across all API routes
- Upload modal path selector: `raw/` (AI ingest, default) vs `wiki/` (direct, appears in tree immediately)
- Reindex converted from sync to streaming NDJSON response with per-file progress
- Introduced `structure.json` as the authoritative vault layout schema stored in S3 — reindex and tree building respect it, cannot reorganize spaces
- Structure schema improvements deferred to Phase 6 (SaaS) in ROADMAP.md
- "All spaces" option added to both Re-index and Pending tabs

## Outcomes

- **Fixed**: `web/components/sidebar.tsx` — icon wrapper div for pixel parity
- **Fixed**: `web/components/app-shell.tsx` — spaces prop uses folder IDs not display names; filters `__unlisted`
- **Created**: `web/lib/vault-structure.ts` — VaultStructure type + S3 read/write
- **Rewritten**: `web/app/api/reindex/route.ts` — streaming NDJSON with progress, respects structure.json
- **Rewritten**: `web/lib/vault-tree.ts` — uses structure.json as authoritative layout
- **Updated**: `web/app/api/raw/route.ts` — supports `__all` to count root + per-space raw files
- **Updated**: `web/app/api/upload/route.ts` — accepts `path` field (raw/wiki)
- **Updated**: `web/components/upload-modal.tsx` — path selector, streaming progress, "All" pill
- **Updated**: `web/components/doc-toolbar.tsx` + `doc-reader.tsx` — upload button added
- **Uploaded**: `s3://vaultmark/project-vaultmark/structure.json` with 3 spaces (articles, wiki, vaultmark)
- **Found**: 170 raw files pending ingestion at root `raw/` prefix

## Open Items

- Deploy to Vercel preview to verify all 400 fixes work in production
- The 170 raw files in `raw/` need ingest pipeline (Phase 3) to be processed
- Structure schema UI management deferred to Phase 6
- Upload modal "Process pending" (curate) tab not yet tested with `__all`
