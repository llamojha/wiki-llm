# Session: 2026-05-16 12:48

**Date:** Saturday, 2026-05-16T13:46:02+02:00

**Summary:** Designed and implemented the Karpathy-style curation pipeline — moving heavy Bedrock LLM processing from Vercel (which was timing out) to an AWS Lambda invoked asynchronously. Replaced the entire CDK stack (ECS/RDS/Cognito) with a minimal Lambda + IAM setup. Performed code review and fixed all identified issues.

**Key Decisions:**
- Lambda-first architecture (not inline Vercel) due to timeout constraints
- Free-form text with `<file path="...">` XML blocks instead of tool_use — better for multi-file markdown generation, avoids JSON-escaping overhead
- Raw files are immutable, tracked via `_processed.json` manifest (hash-based pending detection)
- Single Bedrock call per source (Karpathy pattern) — LLM outputs ALL wiki pages in one response
- CDK stack gutted entirely — replaced ECS/RDS/Cognito/ALB with just Lambda + IAM role
- Bedrock model: `eu.amazon.nova-2-lite-v1:0` inference profile, 32K max output tokens

**Outcomes:**
- Created `infra/lambda/curate/` — full Lambda package (8 modules, 11 passing tests, 10KB bundle)
- Replaced `infra-cdk/lib/infra-cdk-stack.ts` — Lambda-only CDK stack, deployed successfully
- Created Vercel routes: `/api/curate/start`, `/api/curate/status`, `/api/curate/cancel`
- Updated `/api/raw` for manifest-based pending detection
- Updated `web/components/upload-modal.tsx` — polling-based progress UI
- Lambda deployed: `arn:aws:lambda:eu-central-1:858650446023:function:vaultmark-curate`
- IAM permission granted: `vaultmark-vercel` can invoke the Lambda
- Code review performed — fixed 7 issues (regex state bug, cancel not working, self-fetch, race condition, N+1 reads, frontmatter parsing, missing guard)
- Created `CODEX_CONTEXT.md` for migration to Codex

**Open Items:**
- Add `CURATE_LAMBDA_ARN` env var to Vercel project and redeploy
- Redeploy Lambda after code review fixes: `cd infra-cdk && npx cdk deploy`
- End-to-end test with a real file once Vercel is redeployed
- Remove old `web/lib/ingest/` directory after confirming Lambda works
- Note: `.env.local` says `VAULT_REGION=us-east-1` but Lambda/CDK use `eu-central-1` — verify which is correct for the S3 bucket
