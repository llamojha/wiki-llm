apiVersion: v1
kind: Service
metadata:
  name: canopy
  namespace: canopy
  labels:
    app: canopy
spec:
  type: ClusterIP
  selector:
    app: canopy
  ports:
    - name: http
      port: 80
      targetPort: http
